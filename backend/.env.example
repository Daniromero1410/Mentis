# ===========================================
# CONFIGURACION DE ENTORNO - BACKEND
# ===========================================
# Copia este archivo como .env y configura las variables

# Base de datos PostgreSQL
# En Railway: se genera automaticamente como DATABASE_URL
DATABASE_URL=postgresql://usuario:password@localhost:5432/nombre_db

# Clave secreta para JWT (CAMBIAR EN PRODUCCION)
# Genera una clave segura: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu-clave-secreta-muy-segura-cambiar-en-produccion

# Tiempo de expiracion del token en minutos (default: 480 = 8 horas)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Modo debug (false en produccion)
DEBUG=false

# URLs permitidas para CORS (separadas por coma)
# Agregar la URL de tu frontend en Vercel
CORS_ORIGINS=http://localhost:3000,https://tu-app.vercel.app
